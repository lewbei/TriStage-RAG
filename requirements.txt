# TriStage-RAG dependencies are declared in pyproject.toml.
#
# Install everything (core + benchmark + web UI + dev tools):
#
#     pip install -e ".[all]"
#
# Or pick only the extras you need, e.g.:
#
#     pip install -e ".[benchmark]"    # MTEB / LIMIT evaluation
#     pip install -e ".[webui]"        # Flask UI + PDF/DOCX parsing
#     pip install -e ".[dev]"          # pytest only
#
# This file is kept for backwards compatibility with `pip install -r requirements.txt`.
-e .[all]
